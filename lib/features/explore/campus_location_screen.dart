import 'package:flutter/material.dart';

class CampusLocationScreen extends StatelessWidget {
  const CampusLocationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('CampusLocationScreen')));
  }
}
